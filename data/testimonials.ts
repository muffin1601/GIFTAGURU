import type { Testimonial } from "@/types";

/**
 * Real customer testimonials only.
 *
 * This list previously held four invented quotes with fictional names and
 * companies ("Nimbus Technologies", "Solace Financial", ...). Publishing
 * fabricated social proof on a live storefront misrepresents the business, so
 * they have been removed rather than restyled.
 *
 * Add real, attributable quotes here and the homepage section renders itself.
 * While the list is empty the section is skipped entirely.
 */
export const testimonials: Testimonial[] = [];
